<?xml version="1.0" encoding="utf-8"?>
<manifest package="test.com.my"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/eart"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.SceneryActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity"></activity>
        <activity android:name=".ui.TextActivity" android:windowSoftInputMode="adjustUnspecified|stateHidden" ></activity>
        <service android:name=".service.Music"></service>
    </application>

</manifest>
